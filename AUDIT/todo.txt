# TODO Técnico - Auditoría de Arquitectura

[HIGH PRIORITY - SECURITY]
- [x] Configurar Security Headers (CSP, HSTS, X-Content-Type-Options, Referrer-Policy, X-Frame-Options) en `next.config.ts`.
- [ ] Implementar protección de Idempotencia en Server Actions (e.g. `createTransaction`).

[HIGH PRIORITY - UI/UX DEBT]
- [x] Eliminar todos los atributos `style={{}}` en `TransactionForm.tsx`.
- [x] Eliminar todos los atributos `style={{}}` en `TransactionList.tsx`.
- [x] Eliminar todos los atributos `style={{}}` en `BankAccountForm.tsx` y `BankAccountList.tsx`.
- [x] Eliminar todos los atributos `style={{}}` en `DigitalWalletForm.tsx` y `DigitalWalletList.tsx`.
- [x] Eliminar todos los atributos `style={{}}` en `GoalForm.tsx`, `GoalList.tsx` y `QuickAllocate.tsx`.
- [x] Eliminar todos los atributos `style={{}}` en `ContactForm.tsx` y `ContactList.tsx`.

[MEDIUM PRIORITY - ARCHITECTURE (DAL)]
- [x] Crear la capa DAL (Data Access Layer) en `src/features/transactions/data/repository.ts`.
- [x] Refactorizar `createTransaction` action para consumir el DAL en lugar de llamar a `db` directamente (manteniendo la transacción ACID).
- [ ] Extender DAL al resto de entidades (`bank-accounts`, `digital-wallets`, `contacts`, `goals`).

[PHASE 5 - UX & LAYOUT REFACTOR]
- [x] Refactorizar Navbar a Sidebar fijo en Layout.
- [x] Dividir `page.tsx` en múltiples rutas: `/transactions`, `/wealth`, `/contacts`, `/accounts`.
- [x] Purgar `<Flex>` component y estilos en línea de todas las features.
- [x] Mejorar el componente `Table.tsx` para aceptar `rowActions`.

[PHASE 6 - UX REFINEMENT & ADVANCED FLOWS]
- [ ] Crear página dedicada para Metas y Objetivos (`/goals`).
- [ ] Mover Tarjetas de Crédito a la página `/accounts` (junto a Bancos) separadas visualmente.
- [ ] Implementar componente `Tabs` (Solapas) para dividir páginas en sub-secciones.
- [ ] Añadir capacidad de Filtrado (Filter) a las tablas (ej: búsqueda por texto).
- [ ] Mejorar UI de tablas vacías (Empty State con fondo y estructura en vez de solo texto).
- [ ] Eliminar texto redundante "(Opcional)" del campo Ticker en `AssetForm` y diccionarios.
- [ ] Crear página de Configuración (`/settings`) y mover allí los selectores de Idioma y Tema.
- [ ] Añadir un "Placeholder de Usuario" en el Sidebar (preparación para Auth).
- [ ] **Workflow de Transacciones**: Planificar e implementar estado de "Pendiente de Confirmación" para el receptor (Bandeja de entrada de transacciones).
